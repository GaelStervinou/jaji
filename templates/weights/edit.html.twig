{% extends 'base.html.twig' %}

{% block title %}Edit Weights{% endblock %}

{% block body %}
    <h1>Edit Weights</h1>

    {{ include('weights/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_weights_index') }}">back to list</a>

    {{ include('weights/_delete_form.html.twig') }}
{% endblock %}
