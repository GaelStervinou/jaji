<form method="post" action="{{ path('app_weights_delete', {'id': weight.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weight.id) }}">
    <button class="btn">Supprimer</button>
</form>
