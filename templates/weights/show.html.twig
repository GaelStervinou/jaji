{% extends 'base.html.twig' %}

{% block title %}Poids{% endblock %}

{% block body %}
    <div class="flex justify-between flex-row items-center mb-12 max-lg:flex-col max-lg:mb-6">
        <h1 class="text-2xl font-bold max-lg:w-full">Poids</h1>
        <div class="flex gap-4">
            <a href="{{ path('app_weights_index') }}">
                <button class="btn bg-primary-cm-200 text-primary-cm transition-colors duration-300 hover:bg-primary-cm hover:text-white">Retour à la liste</button>
            </a>
            <a href="{{ path('app_weights_edit', {'id': weight.id}) }}">
                <button class="btn bg-primary-cm-200 text-primary-cm transition-colors duration-300 hover:bg-primary-cm hover:text-white">
                    <span class="material-symbols-rounded">
                        edit
                    </span>
                </button>
            </a>
            {{ include('weights/_delete_form.html.twig') }}
        </div>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ weight.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ weight.value }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ weight.date ? weight.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
